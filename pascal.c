#include <stdio.h>
int main ()
{
    //this program will show pascal triangle
    int rows = 0, i = 0, j = 0, num = 1 , space = 0;

    printf("Enter the number of rows for Pascal's Triangle: ");     //number of rows in pyramid
    scanf("%d", &rows);

    for(i = 0; i <= rows; i++)                                     //current row in the triangle=i
        for (space = 1; space <= rows - i; space++)                // space to form pyramidal structure
        {
            printf("  "); 
        }
            for (j=0; j=i;j++)
            {
               
            
            }


}
    
    